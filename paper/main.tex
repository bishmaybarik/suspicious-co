\documentclass[11pt]{article}

\usepackage[margin=1in]{geometry}
\usepackage[protrusion=true,expansion=false]{microtype}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{amsmath}
\usepackage{array}
\usepackage{longtable}
\usepackage{threeparttable}
\usepackage{caption}
\usepackage{float}
\usepackage{pdflscape}
\usepackage{xcolor}
\usepackage{xspace}
\usepackage[hidelinks]{hyperref}

\graphicspath{{outputs/final/figures/}}
\input{outputs/final/tables/numbers.tex}

\definecolor{paperblue}{HTML}{356A9A}
\hypersetup{
  pdftitle={Mapping the First Hop},
  pdfauthor={Canonical research synthesis},
  colorlinks=true,
  linkcolor=paperblue,
  urlcolor=paperblue
}
\captionsetup{font=small,labelfont=bf}
\setcounter{secnumdepth}{3}
\setcounter{tocdepth}{2}

\newcommand{\pct}{\%}
\xspaceaddexceptions{\pct}
\newcommand{\pp}{percentage points}
\newcommand{\sample}{the supplied \Parents-parent dataset}
\newcommand{\FloatBarrier}{\clearpage}

\title{\textbf{Mapping the First Hop: Denominators, Hierarchy, and Gateway
Structure in Reported Indian Foreign Ownership Networks}}
\author{Canonical research synthesis}
\date{August 2026}

\begin{document}

\maketitle

\begin{abstract}
A registered foreign investment is the first hop of a corporate structure, not
the structure itself. This paper reconstructs what sits beneath the first hop
for \Parents\ Indian parent buckets in a supplied research dataset. The source
is not a firm cross-section: \RawRows\ candidate source rows reduce to
\PreferredRows\ preferred target-years, \Targets\ structural target
occurrences, \ParentEntities\ parent-scoped normalized entity candidates, and
\GlobalEntities\ global normalized candidates. Keeping these denominators
separate changes both rankings and interpretation. The \UINs\ mapping channels
in the file expand into \Targets\ foreign targets---a mean of
\TargetsPerUinMean\ and a median of \TargetsPerUinMedian\ targets per
channel---and \DepthTwoPlusPct\pct\ of targets sit two or more reported levels
below the Indian parent. That structure is highly concentrated. The modal
channel contains \ModalUinPct\pct\ of targets pooled and \ModalUinEqualPct\pct\
under equal-parent weighting; among \ConcentrationParents\ sufficiently large
groups the median largest observed subtree contains
\ObservedSubtreeMedian\pct\ of normalized entities, and
\DagDominatorMedian\pct\ under a strict directed-acyclic-graph dominator
definition. How much structure follows also depends on where the first hop
lands: Dutch first-hop entities have a mean of \DutchGatewayMean\ downstream
entities (parent-equal mean \DutchGatewayEqualMean, leave-one-parent-out
\DutchGatewayLooLow--\DutchGatewayLooHigh) against \DutchGatewayUsMean\ for the
much more numerous United States first hops. Direct relations are also commonly
international: \CrossBorderEdges/\NonrootEdges\ nonroot edges
(\CrossBorderPct\pct) cross a jurisdiction boundary, with an equal-parent
estimate of \CrossBorderEqualPct\pct. These results survive entity
de-duplication, parent balancing, leave-one-parent-out checks, and alternative
network definitions. Other apparent patterns do not: reported level and
reconstructed graph distance disagree for \DepthMismatchCount/\CompletePaths
complete paths, and pooled depth, sector, ownership, and financial patterns are
driven by parent composition or measurement limits. The paper therefore
establishes a measurement-corrected topology for this dataset, and separates it
explicitly from the legal, tax, and regulatory interpretations that would
require external evidence.
\end{abstract}

\noindent\textbf{Keywords:} ownership networks; corporate hierarchy;
subsidiary geography; entity resolution; robustness; denominator audit

\vspace{0.8em}
\noindent\textbf{Scope note.} Every quantitative claim below is produced by the
pipeline in this repository from one immutable input, and every number in the
text is a generated macro rather than a transcribed figure. The paper carries no
literature review and no institutional or regulatory interpretation: those would
require independently sourced evidence, which is deliberately outside the
verified base used here. Table~\ref{tab:boundary} states that separation, and
Section~\ref{sec:limitations} lists what a sourced revision would need to
settle.

\tableofcontents
\newpage

\section{Introduction}

When a firm registers a foreign investment, what gets recorded is one company
in one country. What the firm actually owns is usually a structure: the
registered company holds other companies, which hold others in turn, often in
different jurisdictions. Any measure built on the registered entity---an
aggregate of outward investment, a valuation of a foreign affiliate, an index
of corporate complexity---therefore measures the top of something whose size
and shape are not observed at the point of registration.

This paper asks how large and how concentrated that hidden structure is, using
a supplied research dataset that reconstructs reported ownership below the
first foreign hop for \Parents\ Indian parent groups. Three facts frame the
exercise. The \UINs\ registration or mapping channels in the file expand into
\Targets\ reported foreign targets, a mean of \TargetsPerUinMean\ and a median
of \TargetsPerUinMedian\ targets per channel. \DepthTwoPlusPct\pct\ of those
targets sit two or more reported levels below the Indian parent, so the
majority of what the first hop stands for is not the first hop. And the
structure below is not a broad tree but a narrow one: for the median large
group in this sample, a single observed company sits above roughly two-fifths
of the group's entire foreign network.

Answering that question well is mostly a measurement problem, and the paper
treats it as one. Datasets of this kind combine objects that look
interchangeable but are not: source records, reporting years, registered
investments, named subsidiaries, parent relations, and complete ownership
paths. Treating any one of them as ``the number of firms'' mechanically changes
which parent groups and which jurisdictions dominate a result. Hierarchy adds a
second trap. A location can be prominent because many entities reside there,
because a few entities there have many descendants, or because one unusually
large group repeats one route many times. Those are three different empirical
statements, and only the second is about intermediation.

The central question is therefore bounded deliberately: \emph{how do the parent
groups in \sample\ organize their reported foreign ownership networks across
mapping channels, hierarchy, and jurisdictions, and which apparent regularities
survive correcting units, duplicates, parent weights, missing nodes, and graph
definitions?} The goal is measurement-corrected description of these groups.
The data do not identify why a structure exists, whether it is current, what
legal function a named entity performs, or whether any route serves a tax or
regulatory purpose.

The analysis makes five contributions within that boundary. First, it builds an
explicit denominator ladder from source observations to target, entity, edge,
path, mapping-channel, and parent units, and shows that the choice reverses
parent rankings. Second, it reconstructs a parent-scoped directed ownership
graph that retains named-but-unscraped parents together with their supplied
countries, rather than discarding the only information those nodes carry.
Third, it separates three jurisdiction roles that are routinely conflated:
residence, unique intermediary presence, and descendant-weighted path exposure.
Fourth, it reports pooled, equal-parent, leave-one-parent-out, and
alternative-definition estimates side by side for every headline number, so
that a reader can see which results are properties of the network and which are
properties of the largest parent group. Fifth, it treats failed hypotheses as
results: several initially striking depth, sector, ownership, and financial
relationships do not survive the relevant correction, and the paper reports
them as negative findings rather than dropping them.

Four substantive results follow. Parent architectures are heterogeneous along
axes that cannot be collapsed into one defensible complexity ranking: the
largest network in the sample is reported to reach level 12, another network
less than half its size reaches the same level, and a third with almost two hundred
entities never goes below level 2. Networks are concentrated behind a dominant
mapping channel or one large observed subtree, and that concentration is
similar under four different definitions of ``dominant.'' Where the first hop
lands predicts how much structure sits below it: Dutch first hops have a mean
of \DutchGatewayMean\ downstream entities against \DutchGatewayUsMean\ for the
twice-as-numerous United States first hops, and the ordering survives parent
balancing and omission of any single parent. Finally, a majority of reported
child--parent relations below the first hop cross a jurisdiction boundary
(\CrossBorderPct\pct), which is the single most definition-stable statistic in
the study.

Three results in the opposite direction matter as much. Reported hierarchy
level and reconstructed graph distance disagree for
\DepthMismatchCount/\CompletePaths\ complete paths, and one parent group
supplies \MothersonMismatchShare\pct\ of the disagreements, so ``depth'' is not
one variable. An apparent sector--depth pattern is two groups. An apparent
tendency for ownership stakes to be more complete deeper in the hierarchy is
parent composition. And the financial layer is sparse, unit-less, and partly
built from reused extracted evidence, so it supports an audit of data quality
rather than any comparison of firm performance.

Throughout, the paper distinguishes what this dataset establishes from what it
cannot. Unusual topology is not evidence of wrongdoing, a country label is not
a statement about tax residence or entity function, and a maintained list of
``financial centres'' is an imposed classification rather than an observed
variable. Table~\ref{tab:boundary} states that separation explicitly, and the
paper is written to respect it.

The remainder proceeds as follows. Section~\ref{sec:data} defines the units and
Section~\ref{sec:methods} the graph, paths, and estimands.
Section~\ref{sec:architecture} documents the denominator correction and
architectural heterogeneity; Section~\ref{sec:channels} the expansion of
mapping channels and the concentration of networks behind dominant channels and
subtrees; Section~\ref{sec:geography} the geography of intermediation.
Section~\ref{sec:depth} confronts the two depth measures,
Section~\ref{sec:financial} audits the ownership and financial fields, and
Section~\ref{sec:robustness} collects the robustness evidence.
Section~\ref{sec:limitations} states the interpretation boundary and the
questions this file cannot settle.

\section{Data and analytical units}
\label{sec:data}

\subsection{Source structure and scope}

The immutable input is a Stata file containing candidate financial-statement
sources attached to reported foreign targets. The file has \RawRows\ rows and
\Jurisdictions\ standardized jurisdiction labels across \Parents\ supplied
ultimate-parent buckets. Rows are not unique companies. A target can recur for
multiple fiscal years and can have multiple candidate sources within a year.
The upstream preferred-source flag selects \PreferredRows\ target-year rows,
but structural fields are invariant within each target identifier. The
structural cross-section therefore contains \Targets\ target occurrences.

The sample is not accompanied by a sampling frame for all Indian outward
investment. No result below is generalized beyond the supplied parent buckets.
Similarly, the file provides reported names and ownership relationships, not a
complete legal-entity registry or verified historical ownership charts.

\begin{table}[tbp]
\centering
\caption{The canonical denominator ladder}
\label{tab:denominators}
\small
\input{outputs/final/tables/paper_01_denominators.tex}
\begin{minipage}{0.94\textwidth}
\footnotesize\vspace{0.4em}
\textit{Notes:} Counts are generated from the immutable source by the
canonical pipeline. A target occurrence is one reported ownership-path target.
Normalized entities are conservative name--country candidates, not verified
legal identities. A complete observed path reaches the Indian parent through
observed entity nodes.
\end{minipage}
\end{table}

\begin{figure}[tbp]
\centering
\includegraphics[width=\textwidth]{figure_01_denominators_and_depth.pdf}
\caption{Six analytical units, and two depth measures on their own denominators}
\label{fig:denominators}
\begin{minipage}{0.94\textwidth}
\footnotesize\textit{Notes:} Panel (a) uses a logarithmic count scale because
the units differ sharply in size. Panel (b) compares the supplied reported
level for all targets with graph distance among complete observed paths; these
are not interchangeable measures.
\end{minipage}
\end{figure}

\subsection{Targets, normalized entities, and mapping channels}

The primary path and exposure unit is the target identifier. For within-parent
architecture, entity names are normalized conservatively: Unicode is reduced
to ASCII, case and punctuation are removed, ampersands are standardized, and
the supplied country is spelling-harmonized. A parent-scoped entity key is the
combination of ultimate-parent bucket, normalized name, and standardized
country. This yields \ParentEntities\ candidates, only slightly fewer than the
\Targets\ targets. Dropping the parent component yields \GlobalEntities
global candidates. The difference is small in aggregate, but the estimands are
conceptually distinct: a shared venture may be one global entity while still
representing exposure in more than one parent network.

The file also contains \UINs\ UIN values. They are retained as supplied and
treated only as registration or mapping channels. The analysis does not infer
the semantics of their substrings, a registration date, or legal-entity
identity. \ObservedRoots\ UINs have an observed level-0 target, so two mapping
channels lack an observed first-hop entity in this file.

\subsection{Financial observations}

Financial variables remain at the preferred target-year level. Coverage gates
are reported separately from network construction. A target has a located
source if any source row carries the supplied source-found indicator, a parsed
balance sheet if equity is numeric in any row, and a valuation-ready record if
the upstream ready flag is present at least once. Absolute monetary values are
not pooled across currencies or unspecified units.

\section{Ownership-network construction and estimands}
\label{sec:methods}

\subsection{What the hierarchy is}

It helps to fix the object before measuring it. Each parent group is rooted at
one Indian company. Directly beneath it sit the first foreign hops, recorded at
level 0: these are the companies the Indian parent is reported to hold
directly. A level-1 target names a level-0 company as its immediate parent, a
level-2 target names a level-1 company, and so on. A group's network is
therefore a set of chains

\[
  \text{Indian parent} \;\rightarrow\; \underbrace{E_0}_{\text{level 0}}
  \;\rightarrow\; E_1 \;\rightarrow\; \cdots \;\rightarrow\; E_k ,
\]

where each arrow is a reported immediate-parent relation and each $E_j$ carries
its own country label. Nothing constrains consecutive entities to share a
country: an Indian parent may hold a Dutch company that holds a German company
that holds a Brazilian one. Nothing constrains the shape either. A group can be
wide and flat, with many level-0 entities and no further layers, or narrow and
deep, with one level-0 entity above a chain that continues for ten more steps.
Which of those a group looks like is the architectural variation this paper
measures.

Two properties of that structure drive the rest of the analysis. First,
descendants accumulate: an entity high in a chain has every entity below it in
its subtree, so counting ``entities below jurisdiction $X$'' counts a shared
ancestor once for each descendant. Second, the same target can be counted as an
occurrence, as a normalized entity, or as a path, and those three counts differ.
The estimands defined below keep them apart.

\subsection{Edge resolution}

Each structural target supplies an entity name and country, reported level,
immediate-parent name and country, UIN, and ultimate-parent bucket. Candidate
parents are formed within the same ultimate-parent bucket from conservative
name aliases. Candidate matches are ranked lexicographically by whether they
occupy the expected preceding level, whether the supplied parent country
matches, whether the original names match exactly, and whether the UIN matches.
The canonical build accepts no fuzzy match.

The result is one occurrence-level edge for each target. Level-0 targets link
to an explicit Indian ultimate-parent node. Below level 0, \ExpectedLinks\ of
\NonrootEdges\ relations link to an observed entity at the expected prior
level, \OtherLevelLinks\ link to an observed entity at another reported level,
and \MissingParentEdges\ terminate at one of \MissingParentNodes\ normalized
named-but-unscraped parent nodes. These unresolved nodes retain their supplied
country. Discarding the country would understate upstream exposure for some
groups; treating the node as observed would overstate path completeness.

Occurrence edges are collapsed to a logical entity graph for subtree and
gateway calculations. The parent-specific normalized graphs are directed and
acyclic under the accepted links. The graph preserves alternative reported
parents where they exist rather than forcing a tree.

\subsection{Paths and depth}

For every target, recursion follows observed parents toward the ultimate
parent. A path is complete only when it reaches that root through observed
entities. This succeeds for \CompletePaths/\Targets\ targets
(\CompletePathPct\pct). A path that encounters a named missing node remains
useful for country exposure up to that node but is not declared complete.

The supplied reported level and reconstructed graph distance are stored
separately. Reported level may encode a hierarchy with nodes missing from the
target table. Conversely, cross-channel name matching can create a shorter
observed route than the reported sequence. Without external ownership charts,
neither measure is relabeled as ``true'' depth.

\subsection{Network concentration}

Four concentration quantities answer different questions.

\begin{itemize}
  \item The \emph{modal-UIN share} is the fraction of a parent's targets
  assigned to its largest UIN channel.
  \item The \emph{largest observed subtree} counts normalized entity
  descendants below the observed entity with the largest descendant set,
  divided by the other entities in that parent network.
  \item The \emph{named-node-inclusive subtree} also allows an unresolved
  named parent to be the top node, while still not calling the path complete.
  \item The \emph{strict DAG dominator} removes an observed node and counts the
  entity nodes no longer reachable from the ultimate parent. It therefore
  respects alternative routes that bypass the candidate node.
\end{itemize}

The subtree summaries use the \ConcentrationParents\ parent groups with at
least fifteen targets. That threshold prevents very small groups from
determining a median while retaining most of the structural sample. Subtree
language describes the mapped topology. It does not establish legal control,
cash-flow dependence, or an operational chokepoint.

\subsection{Geographic roles}

The analysis distinguishes three jurisdiction roles. \emph{Residence} counts
normalized entities located in a jurisdiction. An \emph{observed
intermediary} is a unique observed entity that appears as the parent of at
least one logical edge. A jurisdiction is \emph{strictly upstream} of a target
if it appears above the terminal target in the reconstructed path; the target's
own residence is excluded from its exposure set. India is excluded as the
ultimate root in foreign-jurisdiction role and cover calculations.

A \emph{gateway} is an observed level-0 entity. Gateway amplification is the
number of normalized entity descendants below it. Jurisdiction means weight
gateways; parent-equal means first average gateways within parent and then
average parents. A robustness subset retains gateways for which no single
direct-child branch contains a majority of descendants.

An edge is cross-border when the standardized child and immediate-parent
country labels differ. The cross-border statistic excludes level-0 links to
India, which would otherwise be cross-border by construction.

\subsection{Weighting and robustness}

For a binary target outcome $y_i$ and parent $p(i)$, the pooled estimand is
$N^{-1}\sum_i y_i$. The equal-parent estimand is

\[
  \frac{1}{P}\sum_{p=1}^{P}\left(\frac{1}{N_p}\sum_{i:p(i)=p} y_i\right).
\]

The former describes target-weighted exposure in this file; the latter gives
each parent bucket the same weight. Leave-one-parent-out estimates repeat the
relevant pooled or fixed-effect calculation after excluding each parent in
turn. Where units differ---entities, edges, paths, gateways, or parents---the
paper labels the denominator explicitly rather than comparing percentages as
though they shared an estimand.

\section{Denominators and parent architecture}
\label{sec:architecture}

\subsection{Raw-row weighting changes the sample composition}

The denominator correction is empirically first order, and the cleanest
demonstration is that it reverses a ranking. Tata Communications contributes
\TataCommRawRows\ source rows but only \TataCommEntities\ parent-scoped
normalized entities. Motherson contributes \MothersonRawRows\ rows but
\MothersonEntities\ normalized entities. The former is therefore the largest
group in the file by source rows and roughly a sixth the size of the latter by
network entities. Nothing about the corporate structures changed between those
two sentences; only the unit did. Any statistic
computed directly on source rows would overweight targets with longer or more
duplicated financial-source histories.

The closeness of the target and normalized-entity totals is reassuring but
does not make the units interchangeable. Four parent-scoped alias collapses
are enough to matter for exact graph topology, and cross-parent shared names
have a different interpretation from within-parent duplicates. The canonical
tables therefore retain target and entity counts side by side.

\subsection{Architecture is multidimensional}

Figure~\ref{fig:parent-architecture} shows that group size, geographic breadth,
reported depth, and channel concentration do not move together. Table
\ref{tab:parent-examples} gives illustrative contrasts. Motherson is the
largest entity network in the sample and has the greatest reported maximum
depth, while Wipro spans more jurisdiction labels. Reliance Industries is
large but shallow in reported levels. Tata Communications is much smaller as
an entity network than its source-row count suggests. ONGC Videsh has many
UIN channels relative to its target count and a shallow observed graph.

These comparisons do not support a one-dimensional complexity index. A group
can be broad but shallow, deep but concentrated behind one entry channel, or
large because one subtree contains many acquired or portfolio entities.
Calling any one of those structures ``more complex'' would impose an
unvalidated weighting across substantively different axes.

\begin{figure}[tbp]
\centering
\includegraphics[width=0.91\textwidth]{figure_02_parent_architecture.pdf}
\caption{Size, jurisdictional breadth, reported depth, and channel concentration by parent group}
\label{fig:parent-architecture}
\begin{minipage}{0.9\textwidth}
\footnotesize\textit{Notes:} Each marker is one supplied ultimate-parent
bucket. Marker color is maximum reported level; marker area increases with the
within-parent modal-UIN target share. Entity count uses the parent-scoped
normalized name--country key.
\end{minipage}
\end{figure}

\begin{table}[tbp]
\centering
\caption{Illustrative parent architectures}
\label{tab:parent-examples}
\scriptsize
\resizebox{\textwidth}{!}{\input{outputs/final/tables/paper_02_parent_examples.tex}}
\begin{minipage}{0.98\textwidth}
\footnotesize\vspace{0.4em}
\textit{Notes:} Graph maximum is reconstructed distance among complete paths.
Modal UIN is the largest UIN's share of target occurrences. Complete paths are
reported as a percentage of the parent's targets. Rows are selected to
illustrate contrasting architectures, not to rank firms.
\end{minipage}
\end{table}

\FloatBarrier

\section{Dominant channels and large subtrees}
\label{sec:channels}

\subsection{How far one mapping channel reaches}

Section~\ref{sec:architecture} showed that parent groups differ. This section
asks what a single registration or mapping channel stands for, and how much of
a group's network hangs from one place.

The \UINs\ channels in the file resolve to \Targets\ reported foreign targets:
a mean of \TargetsPerUinMean\ targets per channel, but a median of only
\TargetsPerUinMedian. The two statistics differ because the distribution is
extremely skewed. \SingleTargetUins\ of \UINs\ channels
(\SingleTargetUinPct\pct) resolve to exactly one target, while the ten largest
channels together account for \TopTenUinPct\pct\ of all targets and the single
largest contains \LargestUinTargets. \ObservedRoots\ of \UINs\ channels have an
observed level-0 target, so two channels lack an observed first foreign hop in
this file, and none has more than one.

Two things follow, and they must be kept apart. As a statement about this
mapping, the expansion factor is large and unevenly distributed: for half of
the channels the first hop is the whole of what is recorded, while a handful of
channels carry most of the entity mass. As a statement about registration
practice or official statistics it establishes nothing, because the file
contains no evidence on how these channels were assigned, what their identifiers
encode, or what share of any register they represent. The paper uses the
expansion only as a description of the supplied mapping.

\subsection{Concentration behind one channel or one subtree}

The largest UIN within each parent accounts for \ModalUinCount/\Targets
targets, or \ModalUinPct\pct\ pooled. Giving every parent equal weight produces
\ModalUinEqualPct\pct; the median parent is \ModalUinMedianPct\pct, and the
pooled leave-one-parent-out interval is narrow (Figure~\ref{fig:robustness}).
Channel concentration is therefore not only an artifact of the largest parent
bucket. At the same time, UIN is an administrative or mapping unit in this
analysis, not a verified company or control node.

Graph concentration yields a complementary result. Across the
\ConcentrationParents\ larger groups, the median largest observed subtree
contains \ObservedSubtreeMedian\pct\ of the other normalized entities. The
median changes only modestly to \DagDominatorMedian\pct\ under the strict DAG
dominator definition, which allows alternative paths to bypass the removed
node. When named-but-unscraped parent nodes may sit at the top of a subtree,
the median rises to \NamedNodeSubtreeMedian\pct.

The shape of that concentration is layering rather than branching. Of the
\ParentEntities\ normalized entities, \HoldNothingPct\pct\ hold nothing at all,
and of the \ObservedIntermediaries\ that hold something, \OneChildHolders
(\OneChildHolderPct\pct) hold exactly one entity. Wide branching is confined to
a few nodes: the \HubNodes\ entities with ten or more direct children originate
\HubEdges\ of the \ObservedParentEdges\ logical edges that begin at an observed
entity (\HubEdgeSharePct\pct). A network can therefore be deep without being
bushy, which is why maximum depth and entity count are separate axes in
Section~\ref{sec:architecture}. Two of the three largest hubs carry names that
identify them as investment funds, so a high out-degree is not by itself
evidence of an operating span of control; the robustness matrix reports the
three-country exposure with those funds' children removed.

Figure~\ref{fig:channels} makes the conceptual separation visible. UIN and
subtree shares are often close, but not always. A mapping channel can contain
many targets whose observed graph is split, while a linked subtree can span
more than one UIN. Missing parents can also reverse the apparent concentration
of a particular group. The defensible result is that dominant mapped channels
and large observed subtrees are common in this sample. It is not that every
network has one legally dispositive company.

\begin{figure}[tbp]
\centering
\includegraphics[width=\textwidth]{figure_03_channel_concentration.pdf}
\caption{Channel concentration under alternative graph definitions}
\label{fig:channels}
\begin{minipage}{0.96\textwidth}
\footnotesize\textit{Notes:} The sample is parent groups with at least fifteen
targets. Subtree shares use normalized entity candidates and exclude the top
node from the denominator. The strict DAG measure counts entities detached
from the ultimate root after removing the candidate node. The hollow marker is
the target-weighted modal-UIN share and therefore uses a different unit.
\end{minipage}
\end{figure}

\FloatBarrier

\section{Geography and intermediary structure}
\label{sec:geography}

Concentration says how much of a network hangs from one node. This section asks
where those nodes are. The order of the evidence matters: the paper first
establishes that layering is international at all, then that first-hop location
predicts how much structure follows, and only then compares jurisdictions --- by
which point it is clear that ``common'' and ``upstream'' are different
properties.

\subsection{Cross-border ownership relations}

Among \NonrootEdges\ reported child--parent relations below level 0,
\CrossBorderEdges\ cross a standardized jurisdiction boundary. The pooled
share is \CrossBorderPct\pct, the equal-parent mean is
\CrossBorderEqualPct\pct, and the leave-one-parent-out range is
\CrossBorderLooLow--\CrossBorderLooHigh\pct. Recomputing with the raw supplied
country labels gives essentially the same result (Table~\ref{tab:robustness}).
This is one of the most definition-stable structural facts in the study.

The interpretation remains modest. A cross-border edge says only that the
reported child and immediate parent carry different country labels. It does
not reveal operating location, management, tax residence, legal control, or
the purpose of the relation.

\subsection{First-hop gateway amplification}

Table~\ref{tab:gateways} and Figure~\ref{fig:gateways} treat each observed
level-0 entity as a gateway and count normalized descendants below it. Dutch
gateways have a mean of \DutchGatewayMean\ descendants and median of
\DutchGatewayMedian. The parent-equal mean is \DutchGatewayEqualMean, and the
pooled leave-one-parent-out means range from \DutchGatewayLooLow\ to
\DutchGatewayLooHigh. Restricting to gateways without a single majority child
branch gives a mean of \DutchGatewayRobustMean, so the average is not generated
only by one immediate acquisition branch per gateway.

Mauritian gateways also show greater amplification than the United States
comparison (mean \MauritiusGatewayMean, median \MauritiusGatewayMedian),
although their median is well below their mean. The contrast with the United
States is the sharpest in the table: \UsGateways\ United States gateways, more
than twice the Dutch count and spanning as many parent groups, carry a mean of
only \DutchGatewayUsMean\ descendants and a median of \UsGatewayMedian. Being a
frequent first destination and being a position from which a network extends
are, in this sample, different things.

Three properties keep this from being one group's architecture. The
\DutchGateways\ Dutch gateways span \DutchGatewayParents\ different parent
groups; the parent-equal mean exceeds the gateway-weighted mean, so balancing
groups strengthens rather than weakens the pattern; and the ranking survives
dropping any single parent. The magnitudes, however, are estimand-specific. The
Dutch-to-United-States ratio is roughly three and a half on means and about
eight on medians, because the United States distribution is dominated by
single-entity first hops. Any statement of the form ``$X$ times more'' must name
which of the two it uses.

Interpretation stops there. A first hop with many descendants may be an
acquisition whose target already owned a large group, a financing or governance
vehicle assembled afterwards, an operating headquarters, or an artifact of how
the mapping attributes intermediate layers. This file observes none of those
histories, and the descendant count cannot distinguish them.

\begin{table}[tbp]
\centering
\caption{Downstream amplification by selected first-hop jurisdiction}
\label{tab:gateways}
\scriptsize
\resizebox{\textwidth}{!}{\input{outputs/final/tables/paper_03_gateway_amplification.tex}}
\begin{minipage}{0.97\textwidth}
\footnotesize\vspace{0.4em}
\textit{Notes:} One gateway is one observed level-0 normalized entity. Mean
and median weight gateways. The parent-equal mean first averages gateways
within parent. The LOO range repeats the gateway-weighted mean after omitting
each parent. The table shows selected jurisdictions; the full generated table
contains every jurisdiction with at least three gateways.
\end{minipage}
\end{table}

\begin{figure}[tbp]
\centering
\includegraphics[width=0.93\textwidth]{figure_04_gateway_amplification.pdf}
\caption{Downstream entities per observed first-hop entity, by jurisdiction}
\label{fig:gateways}
\begin{minipage}{0.91\textwidth}
\footnotesize\textit{Notes:} Each blue point is an observed level-0 gateway;
diamonds mark jurisdiction medians. The horizontal axis uses a symmetric-log
display to retain zero-descendant gateways. The plotted jurisdictions each
have at least five observed gateways.
\end{minipage}
\end{figure}

\subsection{Residence, intermediary prevalence, and path exposure}

There are \ObservedIntermediaries\ unique observed parent nodes in the logical
entity graph. Table~\ref{tab:roles} contrasts their distribution with resident
entity counts and target paths. The United States is the largest resident
location among the displayed jurisdictions. The Netherlands and Mauritius,
however, have substantially more target paths below them relative to their
resident counts. Figure~\ref{fig:roles} generalizes that comparison: points
above the diagonal have more descendant paths than resident entities, while
points below it are more terminal in this mapped topology.

The Netherlands is strictly upstream of \DutchUpstreamPct\pct\ of target
paths, compared with \DutchUpstreamEqualPct\pct\ after equal-parent weighting.
The pooled value is therefore partly a function of the sizes of the parent
networks using Dutch routes, but the role does not disappear when parents are
balanced.

The greedy union of the Netherlands, United States, and Mauritius is strictly
upstream of \TopThreeCount/\Targets\ target paths (\TopThreePct\pct). The
equal-parent union is only \TopThreeEqualPct\pct, and removing a large set of
venture-fund children gives \TopThreeNoFundsPct\pct. The gap is substantive:
the pooled cover is a descendant-weighted exposure statistic, not the share of
unique intermediaries and not a counterfactual measure of what would be
disconnected if three jurisdictions vanished.

\begin{table}[tbp]
\centering
\caption{Selected jurisdiction roles under distinct denominators}
\label{tab:roles}
\scriptsize
\resizebox{\textwidth}{!}{\input{outputs/final/tables/paper_04_jurisdiction_roles.tex}}
\begin{minipage}{0.97\textwidth}
\footnotesize\vspace{0.4em}
\textit{Notes:} Residents are parent-scoped normalized entities. Intermediaries
are unique observed parent nodes. Paths below count target paths on which the
jurisdiction appears strictly upstream, including a supplied country on a
named unresolved node. Pooled and parent-equal percentages use target paths;
``Parents'' is the number of parent buckets affected.
\end{minipage}
\end{table}

\begin{figure}[tbp]
\centering
\includegraphics[width=0.88\textwidth]{figure_05_jurisdiction_roles.pdf}
\caption{Resident entities against target paths with the jurisdiction strictly upstream}
\label{fig:roles}
\begin{minipage}{0.88\textwidth}
\footnotesize\textit{Notes:} Both axes are logarithmic. Marker area increases
with the number of parent groups affected. Color is the jurisdiction's share
of unique observed intermediary nodes divided by its share of resident
entities, capped for display. India is excluded as the ultimate root. Only
jurisdictions appearing on the paths of at least five parent groups are
labeled: several unlabeled points in the upper left have very high paths per
resident entity but rest on one or two groups, and are not general patterns.
\end{minipage}
\end{figure}

\subsection{Why a fixed ``centre'' category is not a core result}

A maintained list of frequently discussed financial or intermediary locations
is upstream of \DeclaredCentrePct\pct\ of target paths, and the equal-parent
estimate is \DeclaredCentreEqualPct\pct. But only
\DeclaredCentreNodePct\pct\ of unique observed intermediary nodes fall in the
same list. The path figure repeats a shared upstream node for every descendant,
and the list itself is imposed rather than observed. It also combines
locations that may host operating, financing, acquisition, or holding
entities. The canonical study therefore reports specific measured roles and
keeps the aggregate classification as a labeled sensitivity analysis.

\FloatBarrier

\section{Reported depth and observed graph distance}
\label{sec:depth}

The previous sections used depth loosely, as the number of steps between the
Indian parent and a target. This section shows why that word needs a qualifier
in this dataset, and why the deep tail in particular should not be read as a
sample-wide feature.

Nearly half of targets are reported below the first foreign layer:
\DepthTwoPlusCount/\Targets\ (\DepthTwoPlusPct\pct) have reported level 2 or
higher. The apparently deep tail is less stable. Reported level 5 or higher
contains \DepthFivePlusCount\ targets (\DepthFivePlusPct\pct), while the
equal-parent mean is only \DepthFiveEqualPct\pct. Among complete paths, just
\GraphDepthFivePct\pct\ have reconstructed distance 5 or greater.

The measures disagree directly for \DepthMismatchCount/\CompletePaths
complete paths (\DepthMismatchPct\pct). Motherson contributes
\MothersonMismatchShare\pct\ of these mismatches. Figure~\ref{fig:depth}
shows that the maximum reported and reconstructed depths can diverge sharply
within a group. Reported level may preserve omitted layers; reconstructed
distance may be a lower bound. The concentration of disagreements in one
parent also means that a pooled depth correction cannot be assumed to apply
uniformly.

The sector result provides a falsification exercise. Of
\ManufacturingTargets\ targets labeled general manufacturing,
\DeepManufacturing\ are reported at level 5 or deeper. That looks like a broad
sector-depth association until parent composition is examined: Motherson and
Hindalco account for \DeepManufacturingTopTwo/\DeepManufacturing
(\DeepManufacturingTopTwoPct\pct) of the deep manufacturing targets. The
paper therefore rejects ``manufacturing subsidiaries are generally deeper''
as a sample-wide conclusion.

\begin{figure}[tbp]
\centering
\includegraphics[width=0.91\textwidth]{figure_06_reported_vs_graph_depth.pdf}
\caption{Maximum reported level against maximum reconstructed distance, by parent group}
\label{fig:depth}
\begin{minipage}{0.91\textwidth}
\footnotesize\textit{Notes:} Each row is one parent bucket. Blue markers show
maximum supplied reported level. Orange markers show maximum reconstructed
distance among paths that reach the ultimate parent through observed entity
nodes. The connecting segment is descriptive; neither endpoint is designated
the legal truth.
\end{minipage}
\end{figure}

\FloatBarrier

\section{Ownership fields, financial coverage, and structural proxies}
\label{sec:financial}

Everything so far has used the topology---who holds whom, and where. The file
also carries ownership stakes and extracted financial statements, which would in
principle allow control-weighted and value-weighted versions of the same
statistics. This section explains why neither is estimable here, and reports
the audit as a result in its own right: it bounds what any study built on this
kind of extracted evidence can claim.

\subsection{Recorded stakes and the ownership-depth gradient}

Stake information cannot support a complete control-weighted network. Every
observed level-0 target has missing mapping stake, and \ZeroStakeCount\ of
\NonrootEdges\ nonroot relations record exactly zero (\ZeroStakePct\pct).
Where a preferred financial row contains both mapping stake and an AOC-1
shareholding measure, each comparable zero-stake case is contradicted by a
positive AOC value (Appendix~\ref{app:ownership}). The zeros are therefore
treated as unknown, not as literal absence of ownership.

Restricting to positive-stake edges produces a superficially strong gradient:
the full-ownership rate is \OwnershipLevelOne\pct\ at reported level 1 and
\OwnershipLevelTwoPlus\pct\ at level 2 or deeper, a pooled difference of
\OwnershipPooledGap\ \pp. The equal-parent difference is only
\OwnershipEqualGap\ points, and excluding Reliance reduces the pooled gap to
\OwnershipNoRelianceGap\ points. The stable conclusion is a parent and
portfolio composition effect, not that ownership mechanically strengthens
with depth. Cumulative ownership along full paths is excluded because it would
condition on positive recorded stakes and treat unresolved zeros selectively.

\subsection{Financial attrition and extraction reuse}

Figure~\ref{fig:financial} and Table~\ref{tab:financial} document the financial
coverage funnel. A source is located for \SourceTargets/\Targets\ targets, a
balance sheet is parsed for \ParsedTargets, and only \ReadyTargets\ targets
are ready at least once. The preferred panel contains \ReadyRows\ ready
target-years; \SignPlausibleRows\ pass a positive-assets and nonnegative-
liabilities gate, and only \PlValidRows\ have a testable valid profit-and-loss
identity. Units are blank for \MissingUnitsReady/\ReadyRows\ ready rows.

Duplicate-evidence checks reinforce the limitation. There are
\RepeatedClusters\ repeated signatures defined jointly by fiscal year,
currency, units, assets, liabilities, and equity; they cover \RepeatedRows
parsed rows. In
\EvidenceReuseClusters\ clusters, the same source URL is attached to different
entity names and the core evidence text is byte-identical. A broad union of
repeated-signature and sign flags leaves \BroadUnflaggedRows\ ready rows, but
that remainder is a diagnostic, not a validated analysis sample.

Readiness itself varies greatly across parents, and \ParentsNoReady\ parents
have no ready target. The raw association between reported depth 3+ and
readiness is \FinancialRawDepthGap\ \pp; a parent fixed-effect coefficient is
\FinancialFeDepthGap\ points, with leave-one-parent-out estimates from
\FinancialLooLow\ to \FinancialLooHigh\ points. This is attenuation rather
than elimination, but selection, unit missingness, and extraction reuse make a
financial-performance interpretation indefensible.

\begin{figure}[tbp]
\centering
\includegraphics[width=\textwidth]{figure_07_financial_attrition.pdf}
\caption{Target-level coverage gates and ready-row quality screens}
\label{fig:financial}
\begin{minipage}{0.94\textwidth}
\footnotesize\textit{Notes:} Panel (a) counts structural targets at successive
coverage gates. Panel (b) counts preferred target-year rows. The broad screen
flags either a repeated numeric signature or an implausible sign combination;
its unflagged remainder has not been manually validated.
\end{minipage}
\end{figure}

\begin{table}[tbp]
\centering
\caption{Financial coverage and quality diagnostics}
\label{tab:financial}
\scriptsize
\resizebox{\textwidth}{!}{\input{outputs/final/tables/paper_06_financial_audit.tex}}
\begin{minipage}{0.98\textwidth}
\footnotesize\vspace{0.4em}
\textit{Notes:} The value column is a percentage except for diagnostics whose
unit states otherwise. Repeated numeric signatures are fiscal-year/currency/
units/assets/liabilities/equity clusters. Demonstrated reuse additionally
requires the same URL, different target names, and byte-identical core evidence
text.
\end{minipage}
\end{table}

\subsection{A limited name-based structural proxy}

One supporting result survives a within-cell test. \HoldingNameCount\ targets
contain ``Holding(s),'' ``Holdco,'' or ``Investment(s)'' in the normalized
name. \HoldingNameHolderPct\pct\ of flagged targets hold at least one observed
child, compared with \OtherNameHolderPct\pct\ of other targets. Residualizing
both the outcome and name flag within parent-by-country cells gives a
\HoldingNameFeGap-point association. This suggests that names can be a useful
high-precision, low-recall structural proxy when edges are absent. It does not
verify a legal role, and external role labels and held-out-parent validation
would be required before using it as a general classifier.

\FloatBarrier

\section{Robustness, heterogeneity, and evidence classification}
\label{sec:robustness}

The individual sections reported sensitivities as they arose. Collecting them
in one place answers a different question: which of this paper's numbers are
statements about the network, and which are statements about the composition of
the sample? Figure~\ref{fig:robustness} and Table~\ref{tab:robustness} put the
main estimands on one scale for that comparison.

Three patterns are notably stable. The
cross-border edge share barely moves under equal-parent weighting, raw country
labels, or leave-one-parent-out checks. Modal-UIN concentration also remains
high under parent balance. Dutch gateway amplification remains large under
equal-parent weighting, omission of any one parent, and the no-majority-branch
restriction.

Other estimates change meaningfully. The three-country upstream union and
reported deep share fall under equal-parent weighting. The full-ownership
depth gap shrinks sharply under parent correction. Financial readiness retains
a small negative within-parent depth association, but its LOO range and severe
measurement attrition preclude substantive inference. The maintained-centre
exposure is much smaller when the unit is unique observed intermediary nodes
rather than descendant paths.

No formal probability sample underlies the \Parents\ parent buckets, so the
paper does not present sampling-error $p$-values as though they supported
population inference. Robustness here means sensitivity to measurement and
composition: unique entities versus repeated targets, pooled versus parent-
balanced weights, omission of any one parent, alternative graph definitions,
fund exclusions, and explicit missingness rules.

\begin{figure}[tbp]
\centering
\includegraphics[width=\textwidth]{figure_08_robustness_ladder.pdf}
\caption{Each percent-scale headline estimate under four weightings and definitions}
\label{fig:robustness}
\begin{minipage}{0.96\textwidth}
\footnotesize\textit{Notes:} Grey segments are leave-one-parent-out ranges
where applicable. Blue circles show pooled or primary estimates, orange
squares show equal-parent estimates, and red triangles show the labeled
alternative definition. Rows share a percent scale but not necessarily the
same observation unit; Table~\ref{tab:robustness} states each unit.
\end{minipage}
\end{figure}

\begin{landscape}
\begin{table}[p]
\centering
\caption{Canonical robustness matrix}
\label{tab:robustness}
\scriptsize
\input{outputs/final/tables/paper_05_robustness.tex}
\begin{minipage}{0.86\linewidth}
\footnotesize\vspace{0.4em}
\textit{Notes:} ``Primary'' is pooled unless the row labels a median-parent
estimand. FE denotes a fixed-effect coefficient. LOO repeats the relevant
pooled or fixed-effect estimator after dropping each parent. Alternatives are
not always percentages of the same unit and should be read with the definition
column.
\end{minipage}
\end{table}
\end{landscape}

\section{Limitations and interpretation boundary}
\label{sec:limitations}

The study's limitations are part of its empirical result rather than a generic
disclaimer.

\begin{itemize}
  \item \textbf{Sample scope.} The parent buckets are supplied, not sampled
  from a documented universe. Results describe these groups and cannot
  establish prevalence across Indian multinational firms.
  \item \textbf{Entity identity.} Normalized names and countries are candidate
  identities. They cannot distinguish every alias, reorganization, joint
  exposure, or legally separate same-name company without registration
  identifiers.
  \item \textbf{Hierarchy completeness.} Named missing parents truncate paths,
  and reported levels disagree with observed graph distance. External
  ownership charts are required to adjudicate which representation is legally
  or historically correct.
  \item \textbf{UIN semantics.} The data establish mapping-channel
  concentration but do not validate the meaning of UIN substrings or their
  use as dates, office codes, or ownership types. Vintage claims are therefore
  excluded.
  \item \textbf{Jurisdiction labels.} A country label is not proof of operating
  location, management, tax residence, or entity function. The data cannot
  identify a tax, treaty, financing, acquisition, governance, or operating
  motive.
  \item \textbf{Ownership measurement.} Missing level-0 stakes and contradicted
  zero values prevent control weighting and cumulative-ownership inference.
  \item \textbf{Financial selection.} Sparse coverage, blank units, mixed
  currencies, invalid signs, limited P\&L identities, and demonstrated evidence
  reuse rule out population financial comparisons.
  \item \textbf{Time and current status.} The source combines reported
  structures and source years but does not validate a common current ownership
  date for every edge. The network is a reconstruction of the supplied report,
  not a claim about present legal control.
\end{itemize}

Accordingly, unusual topology is not evidence of wrongdoing. The paper does
not label any company, jurisdiction, route, or transaction fraudulent,
illegal, evasive, abusive, or improper. Legal, regulatory, institutional, and
tax interpretations require external evidence and are outside the identified
design.

\subsection{What this dataset establishes, and what it cannot}

Because the distinction is easy to lose in a long descriptive paper,
Table~\ref{tab:boundary} states it directly. The upper panel lists claims the
reconstruction supports on its own terms: counts, topology, exposure under
named denominators, measurement disagreements, and data-quality rates. The
lower panel lists claims that look adjacent but are not identified here. Each
would require an independently sourced input---company registers, dated
ownership charts, a specification for the mapping identifiers, filing
histories, or a documented sampling frame---and no statistic in this file
substitutes for one. In particular, the frequency of a jurisdiction on
ownership paths carries no information about why entities are located there.

\begin{table}[tbp]
\centering
\caption{The evidence boundary of this study}
\label{tab:boundary}
\scriptsize
\input{outputs/final/tables/paper_07_evidence_boundary.tex}
\begin{minipage}{0.94\textwidth}
\footnotesize\vspace{0.4em}
\textit{Notes:} The table is generated by the analysis pipeline, so the
boundary travels with the results rather than with the prose. Items in the
lower panel are not weaker versions of the upper panel; they are outside the
design and are neither asserted nor denied here.
\end{minipage}
\end{table}

\subsection{Questions this file cannot settle}

Four questions remained open after independent reconstruction and cross-review,
and each is load-bearing for at least one result.

\begin{enumerate}
  \item \textbf{Which depth measure is correct.} Reported level and
  reconstructed distance disagree for \DepthMismatchPct\pct\ of complete paths.
  A reported level may count an intermediary that the file never scrapes, in
  which case graph distance is a lower bound; alternatively, name matching
  across channels may have short-circuited a chain, in which case the reported
  level is stale. External ownership charts are the only resolution. Every
  depth statement in this paper is therefore labeled by measure.
  \item \textbf{What the mapping identifiers encode.} Their fixed format is
  visible in the data, but no supplied documentation defines the fields, so no
  vintage, office, or investment-type reading is used anywhere in the paper.
  \item \textbf{What a recorded zero stake means.} \ZeroStakeCount\ nonroot
  relations record exactly zero, and every comparable case with an independent
  shareholding field contradicts a literal zero. Until the mapping source
  distinguishes unknown from true zero, no control-weighted network statistic
  is safe.
  \item \textbf{Whether an intermediary category can be measured rather than
  assumed.} Classifying a jurisdiction as a ``centre'' by a maintained list is
  an imposed partition; classifying it by the behavior of entities located
  there makes the outcome circular. The name-based proxy in
  Section~\ref{sec:financial} is a partial substitute, not a solution.
\end{enumerate}

\section{Implications for measurement}
\label{sec:implications}

Three implications follow for anyone measuring corporate structure from data of
this kind, and none of them requires an institutional assumption.

First, the registered entity is a weak proxy for the structure it heads. In
this file, half the mapping channels resolve to a single foreign target while
the ten largest resolve to \TopTenUinPct\pct\ of all targets, and
\DepthTwoPlusPct\pct\ of targets sit at least two levels below the parent. A
statistic anchored on the registered entity is not simply a scaled-down version
of the network statistic; the ratio between them varies by a factor of many
across channels, so rescaling by an average multiplier would be misleading.

Second, entity counts and path counts answer different questions and should
never be substituted for one another. The three-jurisdiction upstream union is
\TopThreePct\pct\ of target paths but only \TopThreeEqualPct\pct\ when parents
are balanced, and the maintained-centre category covers \DeclaredCentrePct\pct\
of paths but only \DeclaredCentreNodePct\pct\ of unique intermediary nodes.
Both members of each pair are correct; reporting one and describing the other
is not. The same discipline applies to any complexity index built from entity
counts, which would here be dominated by whichever parent group happens to have
the longest reported chains.

Third, a descriptive result on \Parents\ parent groups is a statement about
those groups. Most pooled statistics in this study move when parents are equally
weighted, two of them by more than half their value, and single groups account
for \MothersonMismatchShare\pct\ of the depth mismatches and for most of the
pooled ownership-depth gap. Leave-one-parent-out
reporting is not a robustness ornament in this setting; it is the difference
between a network fact and a fact about the largest firm in the sample.

\section{Conclusion}

The canonical reconstruction changes the object of study from a
financial-source panel into an explicit set of target occurrences, normalized
entity candidates, edges, and ownership paths. That correction is not cosmetic:
it reverses parent rankings and prevents long source histories from
masquerading as large corporate networks.

Within the supplied sample, the reported structure beneath the first foreign
hop is both substantial and narrow. Mapping channels expand into networks whose
median parent routes a large share of its entities through one observed
company, and the concentration is similar whether it is measured by mapping
channel, observed subtree, named-node-inclusive subtree, or strict graph
dominator. Where the first hop lands is informative about how much follows: the
Netherlands and Mauritius occupy intermediary and gateway positions that their
resident counts alone would not predict, while the United States is prominent
mainly as a destination. A majority of relations below the first hop cross a
jurisdiction boundary, and that result barely moves under any reweighting the
data permit. These are defensible facts about the mapped topology, not about
purpose, legality, or current control.

The negative findings are equally important. Deep manufacturing is almost
entirely a two-parent pattern. The pooled ownership-depth gradient is
composition. Reported depth is not observed graph distance, and the
disagreement is concentrated in one group. The financial layer cannot support
comparative performance claims. A credible study of corporate ownership
networks depends as much on refusing the wrong denominator, and on saying which
attractive result did not survive, as on finding a large statistic.

Future work can extend the evidence boundary only with new validated inputs:
official entity identifiers, dated ownership charts, documented mapping-channel
semantics, verified stake fields, harmonized financial units, filing histories
that distinguish inherited from constructed structure, and a transparent
sampling frame. Until then, the contribution is a reproducible, falsifiable map
of the reported network structure in this dataset, and an explicit account of
where that map ends.

\clearpage
\appendix
\input{paper/appendix.tex}

\end{document}
